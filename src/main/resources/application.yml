spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/waiting_db
    username: root
    password: tjdwls0204!
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true